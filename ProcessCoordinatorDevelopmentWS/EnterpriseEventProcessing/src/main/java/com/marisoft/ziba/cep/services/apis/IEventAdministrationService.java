package com.marisoft.ziba.cep.services.apis;

/** 
 * @author shaf3y
 * Information service which enable administrators to add,
 * remove, and organize channels, event types meta-data (syntax & semantics)
 * and store event data in a rational format rather than using event message
 * based persistence.
 */
public interface IEventAdministrationService {

}
